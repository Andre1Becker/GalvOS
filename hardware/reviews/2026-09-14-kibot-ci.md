# KiBot failure diagnosis — 2026-09-14

Read-only workflow investigation requested by the user. No workflow,
configuration or GitHub settings were changed.

## Confirmed failure

The two KiBot runs for hardware commit
`dcd5d718ce7d57131ba7a3da06afa7a667071112` both fail before generation:

- https://github.com/Andre1Becker/GalvOS/actions/runs/34877691034
- https://github.com/Andre1Becker/GalvOS/actions/runs/34877690255

The log says:

```text
ERROR:Schematic file not found: hardware/schematics/Laser Controller.kicad_sch
No files were found with the provided path: output. No artifacts will be uploaded.
```

The missing schematic path is hard-coded in
`.github/workflows/Workflow.kibot.yaml`. The commit tree contains the renamed
v1 schematic and the v2 schematic, not this requested file.
The upload step has `always()` and `if-no-files-found: error`, so it also
fails after KiBot produces no output. That is a secondary failure.

The same workflow selects the historical `Laser Controller.kicad_pcb`,
not the separate v2 PCB. Its push/PR path filters spell the workflow filename
with lowercase `workflow`, while the actual file begins with `Workflow`.
This filter mismatch is a separate trigger defect, not the failure in these runs.

The action did start successfully with KiBot 1.9.1 and KiCad
10.0.5-testing-188. The observed failure does not establish a KiCad version
incompatibility. Downstream output configuration has not been proven by
these failed runs because generation never reached it.

## Proposed next step — awaiting direction

Use the matching v2 schematic and PCB, correct the workflow path filter,
then rerun and inspect all output steps. Keep draft artifacts explicitly
unreleased. The existing configuration has no ERC/DRC preflight gate;
successful export would not establish production readiness.

The user was asked whether to implement that workflow change. Until answered,
this diagnostic request does not authorize switching manufacturing inputs.
